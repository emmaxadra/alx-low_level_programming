#include "main.h"

/**
 * _abs - main function
 *
 * Description: writing a program
 *
 * @n: integer to make absolute
 *
 * Return: int or int time negative
 *
 */

int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}
